namespace SpellGallery.Enums
{
    /// <summary>
    /// The appearance modes (light/dark mode) of the application
    /// </summary>
    public enum Appearance
    {
        // Undefined
        Undefined = 0,

        // Light Mode
        Light = 1,

        // Dark Mode
        Dark = 2,

        // Use System Setting
        System = 3
    }
}